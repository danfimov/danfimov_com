---
layout: main
---

<header class="pt-2 pb-12">
  <h1 class="text-3xl sm:text-4xl font-bold tracking-tight leading-tight">{{ title }}</h1>
  {% if subtitle != "" %}
  <p class="mt-3 text-lg text-muted dark:text-muted-dark">{{ subtitle }}</p>
  {% endif %}
</header>

{{ content | safe }}
