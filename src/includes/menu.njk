<nav class="menu menu--closed" id="menu" aria-label="Primary">
	<ul class="menu__list">
		{% for item in menu %}
			{% if item.link == page.url %}
				{% set current = 'aria-current="page"' %}
			{% else %}
				{% set current = '' %}
			{% endif %}
			<li class="menu__item">
				<a class="menu__link" href="{{ item.link }}" {{ current | safe }}>
					{{- item.title -}}
				</a>
			</li>
		{% endfor %}
	</ul>



	{% include 'theme-switcher.njk' %}
</nav>
