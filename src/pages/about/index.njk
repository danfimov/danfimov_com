{% from 'intro-picture.njk' import introPicture with context %}

<main class="main column" id="main">
	<header class="caption">
		<h1 class="caption__title caption__title--top">
			{{ title }}
		</h1>
	</header>
	<div class="intro">
		{{ introPicture(context='about') }}

		<p class="intro__who">
			{{ intro.who | safe }}
		</p>
		<p class="intro__path content content-inline">
			{{ intro.path | markdownInline | safe }}
		</p>
		<svg class="intro__scribble" width="200" height="200" aria-hidden="true">
			<use href="/images/scribbles.svg#1"/>
		</svg>
	</div>
	<div class="about-section-list">
		<section class="about-section about-section-list__work">
			<h2 class="about-section__title">
				{{ work.title }}
			</h2>
			<p class="about-section__desc">
				{{ work.desc }}
			</p>
			<ol class="about-section__work about-work" reversed>
				{% for item in work.list %}
					<li class="about-work__item">
						<span class="about-work__period">
							{% set time = item.period.split('–') %}
							<time>{{ time[0] }}</time>–
							{%- if time[1] != '' -%}
								<time>{{ time[1] }}</time>
							{%- endif -%}
						</span>
						<span class="about-work__company">
							{{ item.company }}
							<br>
							<span class="about-work__title">{{ item.title }}</span>
						</span>

						<ul class="about-work__desc">
						{% for item_desc in item.description.items %}
							<li>{{ item_desc | safe }}</li>
						{% endfor %}
						</ul>
					</li>
				{% endfor %}
			</ol>
		</section>

		<section class="about-section about-section-list__education">
			<h2 class="about-section__title">
				{{ education.title }}
			</h2>
			<p class="about-section__desc">
				{{ education.desc }}
			</p>
			<ol class="about-section__education about-education" reversed>
				{% for item in education.list %}
					<li class="about-education__item">
						<span class="about-education__period">
							{% set time = item.period.split('–') %}
							<time>{{ time[0] }}</time>–
							{%- if time[1] != '' -%}
								<time>{{ time[1] }}</time>
							{%- endif -%}
						</span>
						<span class="about-education__company">
							{{ item.company }}
						</span>
						<ul class="about-education__desc">
						{% for item_desc in item.description.items %}
							<li>{{ item_desc | safe }}</li>
						{% endfor %}
						</ul>
					</li>
				{% endfor %}
			</ol>
		</section>

		<section class="about-section about-section-list__contacts">
			<h2 class="about-section__title">
				{{ contacts.title }}
			</h2>
			<p class="about-section__desc">
				{{ contacts.desc }}
			</p>
			<ul class="about-section__contacts">
				{% for item in contacts.list %}
					<li>
						<a class="action" href="{{ item.url }}">
							<svg class="action__icon" width="24" height="24" aria-hidden="true">
								<use href="/images/icons.svg#{{ item.icon }}"/>
							</svg>
							{{- item.title -}}
						</a>
					</li>
				{% endfor %}
			</ul>
			<svg class="about-section__scribble about-section__scribble--contacts" width="200" height="200" aria-hidden="true">
				<use href="/images/scribbles.svg#2"/>
			</svg>
		</section>
	</div>
</main>
